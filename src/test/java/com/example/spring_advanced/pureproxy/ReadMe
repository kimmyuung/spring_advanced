프록시 : 프록시를 사용한 덕분에 코드를 변경할 필요 없이 로그 추적 기능 적용이 가능하였다.

인터페이스 기반 프록시 vs 클래스 기반 프록시
- 인터페이스가 없어도 클래스르 기반으로 프록시 생성이 가능
- 클래스 기반 프록시는 해당 클래스에만 적용이 가능, 인터페이스 기반 프록시는 인터페이스만 같으면 모든 곳에 적용이 가능
- 클래스 기반 프록시는 상속을 사용하기 때문에 몇 가지 제약이 있음
    - 부모 클래스의 생성자 호출 필요
    - 클래스에 final 키워드가 붙으면 상속 불가
    - 메서드에 final 키워드가 붙으면 해당 메서드 오버라이딩 불가

    --> 인터페이스 기반의 프록시를 사용하는 것이 역할과 구현을 명확하게 나누기 때문이 더 좋다
    --> 인터페이스 기반 프록시의 단점은 인터페이스가 필요하다는 그 자체